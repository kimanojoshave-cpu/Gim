# Gim

A clean workspace for software projects.

## Structure

- `src/` — application source code
- `tests/` — tests
- `docs/` — documentation
- `scripts/` — development and utility scripts
- `config/` — project configuration

## Development

Add the project-specific setup and commands here as the project grows.
